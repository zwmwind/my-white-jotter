server:
  port: 8443
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/my_white_jotter?characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      schema-locations: classpath*:sql/init.sql
      data-locations: classpath*:sql/data.sql
      mode: always
debug: true

